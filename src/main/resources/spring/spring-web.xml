<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc
		http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">


    <!-- 设定消息转换的编码为utf-8防止controller返回中文乱码 -->
    <bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter">
        <property name="messageConverters">
            <list>
                <bean
                        class="org.springframework.http.converter.StringHttpMessageConverter">
                    <property name="supportedMediaTypes">
                        <list>
                            <value>text/html;charset=UTF-8</value>
                        </list>
                    </property>
                </bean>
            </list>
        </property>
    </bean>

    <!-- 配置springMVC -->

    <!-- 1：开启Springmvc注解模式 -->
    <!-- 简化配置
     自动注册defaultAnnotationHandlerMapping,AnnotationMethosHandlerAdapter
      提供一系列：数据绑定，数字日期的格式 @numberFormat,@DataTimeFormater,xml,json默认读写支持
      -->
    <mvc:annotation-driven/>

    <!--2： servlet-mapping 映射路径："/" -->
    <!-- 静态资源默认servlet配置
       加入对静态资源的处理： js,gif,png...
       允许使用"/"做整体映射
    -->
    <mvc:default-servlet-handler/>

    <!-- 3：配置jsp显示viewResolver-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 4：扫描web相关的bean包-->
    <context:component-scan base-package="org.sekill.web"/>
</beans>